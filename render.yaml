services:
  - type: web
    name: neuronshell
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: APP_USERS
        sync: false
      - key: SESSION_HOURS
        value: 24
      - key: NODE_ENV
        value: production
